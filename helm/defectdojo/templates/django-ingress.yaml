{{- if .Values.django.ingress.enabled -}}
{{- $fullName := include "defectdojo.fullname" . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    defectdojo.org/component: django
    app.kubernetes.io/name: {{ include "defectdojo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "defectdojo.chart" . }}
    {{- with .Values.extraLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80,"HTTPS":443}]'
    alb.ingress.kubernetes.io/ssl-redirect: "443"
    alb.ingress.kubernetes.io/subnets: 'subnet-047f89f11aba50da8,subnet-00fd52846ff9c5042,subnet-0f921029d1b927b68'
    alb.ingress.kubernetes.io/group.name: prod-eks-defectdojo-int
    alb.ingress.kubernetes.io/group.order: '1000'
    alb.ingress.kubernetes.io/load-balancer-name: prod-eks-defectdojo-int
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:eu-west-1:355098476236:certificate/6f0627a1-1fb7-4f4a-89a8-83af5f74d996
    {{- with .Values.django.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.django.ingress.ingressClassName }}
  ingressClassName: {{ .Values.django.ingress.ingressClassName }}
  {{- end }}
  {{- if .Values.django.ingress.activateTLS }}
  tls:
    - hosts:
        - {{ .Values.host | quote }}
      {{- if .Values.django.ingress.secretName }}
      secretName: {{ .Values.django.ingress.secretName | quote }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.host | quote }}
      http:
        paths:
          - path: {{ .Values.django.ingress.path | default "/" | quote }}
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}-django
                port:
                  name: http
{{- end }}
